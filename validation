class Validation:
    """ This class contains methods for validating email addresses and passwords."""
    
    @staticmethod
    def validate_email(email):
        """Check if the email contains an "@" symbol."""
        return "@" in email

    @staticmethod
    def validate_password(password):
        """
        This method validates a password based on the following criteria:
        - The password must be at least 8 characters long.
        - The password must contain at least one uppercase letter,
          one lowercase letter, one digit, and one special character (!@#$%^&*).

        Args:
          password (str): The password to be validated.

        Returns:
          bool: True if the password is valid, False otherwise.
        """




def example_function():
        print("This is the first line.")
for i in range(5):
        print("This is line", i)  # Indented within the for loop
print("This is the last line.")
        # Check if the password is at least 8 characters long
if len(password) < 8:
            return False

        def my_function():
            # This is the indented block of code
            print("Hello, World!")
    # Your code logic here
    return "Some value"  # This return is inside the function

# Call the function
result = my_function()
print(result)
        
has_upper = False
has_lower = False
has_digit = False
has_special = False
special_characters = "!@#$%^&*"

for char in password:
      def my_function():
    print("Hello")
    print("World")  # Now this line is correctly indented

        # Return True only if all conditions are met
        return has_upper an
